{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start PostgreSQL",
      "type": "shell",
      "command": "docker rm -f papyrus-postgres || true && docker run --name papyrus-postgres -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=papyrus -p 5432:5432 -v papyrus_postgres_data:/var/lib/postgresql/data postgres:17",
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
      }
    },
    {
      "label": "Stop PostgreSQL",
      "type": "shell",
      "command": "docker stop papyrus-postgres",
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
      }
    }
  ]
}
