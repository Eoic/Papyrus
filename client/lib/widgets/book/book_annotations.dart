import 'package:flutter/material.dart';

class BookAnnotations extends StatelessWidget {
  const BookAnnotations({ super.key });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
